
***

# Selenium WebDriver

## What is Selenium WebDriver?

1.  WebDriver is one of the components in Selenium.
2.  WebDriver is a Java interface.
3.  WebDriver is an API (Application Programming Interface).

***

## WebDriver Architecture

    WebDriver (Interface)
            ↓
    RemoteWebDriver (Class)
            ↓
    ChromeDriver, FirefoxDriver, EdgeDriver, etc.

***

## Dependencies

If any dependency is needed, we can download it from:

👉 <https://mvnrepository.com/>

This is the Maven repository where we can download required dependencies.

***

