class is the collection of attributes(variables) and behaviour(methods)
      -->it is a logical entitys
      -->blueprint
      -->it will not occupy any space in memory
objects -->object creation with reference variable->Employee emp = new Employee();
        -->object will only be created in the main method
