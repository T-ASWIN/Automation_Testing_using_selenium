String in java

-->predefined class because its start with capital letter   
-->it also has some predefined methods
-->String name = new String("Aswin");
-->length(Keyword used to find the size of the array)
-->length()(Method used to return size of string)
-->concat()
-->trim()
-->charAt()
-->contains()-->it return true/false the value it check are case-sensitive
-->equals()->case-sensitive  
-->equalsIgnoreCase()->case-insensitive
-->replace(s,r)
-->substring(3,7) ->extract substring from the main string
-->s.toCharArray()-->converting the string to char array
-->




String comparision

s1==s2-->used for comparing objects
s1.equals(s2)-->used for comparing value

